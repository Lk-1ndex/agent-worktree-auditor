interface:
  display_name: "Agent Worktree Auditor"
  short_description: "Audit AI coding changes and context bloat."
  brand_color: "#2563EB"
  default_prompt: "Use $agent-worktree-auditor to snapshot this repo before AI edits and generate a bilingual risk report afterward."

policy:
  allow_implicit_invocation: true

